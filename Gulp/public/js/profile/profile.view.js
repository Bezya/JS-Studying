export default class ProfileView {
    constructor() {
        this.DOMElements = {
        }
    }
    /*
    fillFields() {}

    showPassword() {
        profileService.showPassword(this.DOMElements.userPassword);
        profileService.setBtnName(this.DOMElements.userPassword, this.DOMElements.btnShowPass);
    }

    initListeners() {
        this.DOMElements.btnShowPass.addEventListener("click", this.showPassword.bind(this));
        this.DOMElements.btnGoBack.addEventListener("click", this.backToGallery.bind(this));
    }*/
}